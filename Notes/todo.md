# To-Do List

- Automatic pulling of site icons?
- Add option to open sites in new tab
- After typing url, autofill suggestions for name and icon, but let it be changeable
- Add option to change background color, rotation, size, etc.
- Make sure image preloading works
- Allow escape key to exit input fields
- Fix CSS to not rely on viewHeight.
- Allow " in site names. See https://toml.io/en/v1.1.0#string